# Copy to .env (gitignored) and fill in. chmod 600.
# Any username you like; the password guards a service that can run shell
# commands, so make it a real one.
N8N_BASIC_AUTH_USER=foe
N8N_BASIC_AUTH_PASSWORD=
